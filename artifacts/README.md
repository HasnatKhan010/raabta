# Generated artifacts

Model caches, embeddings, indexes, and metadata are generated locally and are not committed. Exact model identifiers/revisions and generation commands are recorded in configuration and manifests.

